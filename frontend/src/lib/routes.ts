/**
 * Đường dẫn cốt lõi – tránh magic string, dễ bảo trì.
 */
export const ROUTES = {
  login: "/login",
  register: "/register",
  dashboard: "/clm/dashboard",
  editorNew: "/clm/editor/new",
} as const;
